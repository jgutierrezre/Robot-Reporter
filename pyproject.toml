[project]
name = "robot-reporter"
version = "2.0.0"
description = "Parse Robot Framework output.xml and post results to GitHub"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
    "jinja2>=3.1",
]

[project.scripts]
robot-reporter = "reporter:main"
